spring:
  cloud:
    discovery:
      enabled: false
  data:
    mongodb:
      #auto-index-creation: true
      #authentication-database: admin
      host: localhost
      port: 3003
      database: product-db
      #username: greatseo
      #password: greatseo